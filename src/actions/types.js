/**
 * @typedef {Object} PlannerAction
 * @property {string} type
 * @property {Record<string, unknown>} [payload]
 */

/**
 * @typedef {Object} DispatchResult
 * @property {boolean} ok
 * @property {string} [message]
 * @property {boolean} [isError]
 */

export {};
